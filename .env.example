# =============================================================================
# TEAMS CHAT EXPORTER - CONFIGURAÇÕES
# =============================================================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Preencha os valores obrigatórios (CLIENT_ID e TENANT_ID)
# 3. Ajuste as configurações opcionais conforme necessário
#
# ⚠️  NUNCA commite o arquivo .env para o git!
# =============================================================================

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES OBRIGATÓRIAS - Azure AD App Registration
# -----------------------------------------------------------------------------
# Obtenha estes valores no Azure Portal > App registrations > Sua aplicação

# ID da aplicação registrada no Azure AD
CLIENT_ID=your_application_client_id_here

# ID do tenant/diretório do Azure AD  
TENANT_ID=your_directory_tenant_id_here

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES OPCIONAIS - Comportamento da Aplicação
# -----------------------------------------------------------------------------

# Modo de operação:
# - 'test': Exporta apenas o primeiro chat (para testes)
# - 'prod': Exporta todos os chats (padrão)
MODE=test

# Diretório onde os arquivos exportados serão salvos
# Padrão: ./exports (será criado automaticamente)
OUTPUT_DIR=./exports

# Formato adicional de export (além do Excel padrão)
# Opções: json, csv, html
# Padrão: json
EXPORT_FORMAT=json

# Exportar informações de anexos
# true: Inclui metadados dos anexos (padrão)
# false: Ignora anexos para export mais rápido
EXPORT_ATTACHMENTS=true

# Máximo de mensagens por requisição à API
# Valor máximo permitido pela API: 50
# Valores menores podem ser mais estáveis em redes instáveis
MAX_MESSAGES_PER_REQUEST=50

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES AVANÇADAS (raramente precisam ser alteradas)
# -----------------------------------------------------------------------------

# Intervalo entre requisições em segundos (para evitar rate limiting)
# REQUEST_DELAY=0.2

# Timeout para requisições HTTP em segundos
# HTTP_TIMEOUT=30

# Máximo de tentativas em caso de erro
# MAX_RETRIES=3